TODOs:
- add help message
- add version flag
- add release with precompiled binary(for linux_amd64 for now) and sha512 checksum

MAYBEs:
- make enums subcomand and rename to c++ast to be able to add future subcommands?
- parse compile_commands.json?
- add enum parent namespaces to enum name?
